
import { getStructuredText } from 'ember-quickstart/helpers/get-structured-text';
import { module, test } from 'qunit';

module('Unit | Helper | get structured text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getStructuredText([42]);
  assert.ok(result);
});

